<Window x:Class="MoveArc.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="{Binding HeightForm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="{Binding WidthForm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
    <Grid>
        <ItemsControl ItemsSource="{Binding Path=ListShapes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="White" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Ellipse Margin="{Binding Location}" Width="{Binding Width}" Height="{Binding Height}" Stroke="Black" StrokeThickness="3"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
